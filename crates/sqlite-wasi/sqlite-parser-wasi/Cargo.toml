[package]
name = "sqlite-parser-wasi"
version = "0.4.1"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
tolerant-parser-sdk = { path = "../../..", features = ["wasi"] }
sqlite-engine = { path = "../../sqlite/sqlite-engine" }
