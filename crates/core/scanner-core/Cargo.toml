[package]
name = "scanner-core"
version = "0.1.0"
edition = "2024"

[dependencies]
engine-core = { path = "../engine-core" }
thiserror = { workspace = true }

[dev-dependencies]
anyhow = { workspace = true }