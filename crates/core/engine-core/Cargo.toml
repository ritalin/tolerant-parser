[package]
name = "engine-core"
version = "0.1.0"
edition = "2024"

[dependencies]
thiserror = { workspace = true }
serde = { workspace = true }
regex = "1.11.1"
derive_builder = "0.20.2"

[dev-dependencies]
